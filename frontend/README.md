# AI Trip Planner — Frontend

Next.js (App Router) + TypeScript + Tailwind CSS. See the [repo root README](../README.md) and [TUTORIAL.md](../TUTORIAL.md) for the full picture.

```bash
npm install
cp .env.local.example .env.local   # points at the backend API
npm run dev                        # http://localhost:3000
```
